[defaults]
# Allow broken conditionals to work with newer Ansible versions
# This is needed for molecule-plugins compatibility
deprecation_warnings = False
allow_world_readable_tmpfiles = True
host_key_checking = False
# Allow broken conditionals to work with molecule-plugins
ALLOW_BROKEN_CONDITIONALS = True